# Disable directory listing
Options -Indexes

# Deny direct access to JSON and txt files (only data.php can read them)
<FilesMatch "\.(json|txt)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
